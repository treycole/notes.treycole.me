export const Transfer: typeof import("workerpool/types/transfer");
export type Pool = import("workerpool/types/Pool");
export type WorkerPoolOptions = import("workerpool/types/types").WorkerPoolOptions;
export type WorkerRegisterOptions = import("workerpool/types/types").WorkerRegisterOptions;
/**
 * <T>
 */
export type Proxy<T extends {
    [k: string]: (...args: any[]) => any;
}> = import("workerpool/types/types").Proxy<T>;
/**
 * @overload
 * Create a new worker pool
 * @param {WorkerPoolOptions} [script]
 * @returns {Pool} pool
 */
export function pool(script?: import("workerpool/types/types").WorkerPoolOptions | undefined): import("workerpool/types/Pool");
/**
 * @overload
 * Create a new worker pool
 * @param {string} [script]
 * @param {WorkerPoolOptions} [options]
 * @returns {Pool} pool
 */
export function pool(script?: string | undefined, options?: import("workerpool/types/types").WorkerPoolOptions | undefined): import("workerpool/types/Pool");
/**
 * Create a worker and optionally register a set of methods to the worker.
 * @param {{ [k: string]: (...args: any[]) => any }} [methods]
 * @param {WorkerRegisterOptions} [options]
 */
export function worker(methods?: {
    [k: string]: (...args: any[]) => any;
} | undefined, options?: import("workerpool/types/types").WorkerRegisterOptions | undefined): void;
/**
 * Sends an event to the parent worker pool.
 * @param {any} payload
 */
export function workerEmit(payload: any): void;
import { Promise } from "workerpool/types/Promise";
import { platform } from "workerpool/types/environment";
import { isMainThread } from "workerpool/types/environment";
import { cpus } from "workerpool/types/environment";
export { Promise, platform, isMainThread, cpus };
