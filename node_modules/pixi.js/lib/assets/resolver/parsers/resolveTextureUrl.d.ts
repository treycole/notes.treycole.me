import { ExtensionType } from 'pixi.js/lib/extensions/Extensions';
/**
 * A parser that will resolve a texture url
 * @memberof assets
 */
export declare const resolveTextureUrl: {
    extension: {
        type: ExtensionType.ResolveParser;
        name: string;
    };
    test: (url: string, resolvedAsset?: import("pixi.js/lib/assets").ResolvedAsset<import("pixi.js").TextureSourceOptions<any>>, loader?: import("pixi.js/lib/assets").Loader) => boolean;
    parse: (value: string) => {
        resolution: number;
        format: string;
        src: string;
    };
};
