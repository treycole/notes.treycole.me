import { ExtensionType } from 'pixi.js/lib/extensions/Extensions';
import { BatchableSprite } from 'pixi.js/lib/scene/sprite/BatchableSprite';
import type { InstructionSet } from 'pixi.js/lib/rendering/renderers/shared/instructions/InstructionSet';
import type { RenderPipe } from 'pixi.js/lib/rendering/renderers/shared/instructions/RenderPipe';
import type { Texture } from 'pixi.js/lib/rendering/renderers/shared/texture/Texture';
import type { Renderer } from 'pixi.js/lib/rendering/renderers/types';
import type { Text } from 'pixi.js/lib/scene/text/Text';
export declare class CanvasTextPipe implements RenderPipe<Text> {
    /** @ignore */
    static extension: {
        readonly type: readonly [ExtensionType.WebGLPipes, ExtensionType.WebGPUPipes, ExtensionType.CanvasPipes];
        readonly name: "text";
    };
    private _renderer;
    private _gpuText;
    private readonly _destroyRenderableBound;
    constructor(renderer: Renderer);
    resolutionChange(): void;
    validateRenderable(text: Text): boolean;
    addRenderable(text: Text, instructionSet: InstructionSet): void;
    updateRenderable(text: Text): void;
    destroyRenderable(text: Text): void;
    private _destroyRenderableById;
    private _updateText;
    private _updateGpuText;
    private _getGpuText;
    initGpuText(text: Text): {
        texture: Texture<import("pixi.js").TextureSource<any>>;
        currentKey: string;
        batchableSprite: BatchableSprite;
    };
    destroy(): void;
}
