/// <reference types="@webgpu/types" />
import { ExtensionType } from 'pixi.js/lib/extensions/Extensions';
import type { System } from 'pixi.js/lib/rendering/renderers/shared/system/System';
import type { GPU } from 'pixi.js/lib/rendering/renderers/gpu/GpuDeviceSystem';
import type { BindGroup } from 'pixi.js/lib/rendering/renderers/gpu/shader/BindGroup';
import type { GpuProgram } from 'pixi.js/lib/rendering/renderers/gpu/shader/GpuProgram';
import type { WebGPURenderer } from 'pixi.js/lib/rendering/renderers/gpu/WebGPURenderer';
/**
 * This manages the WebGPU bind groups. this is how data is bound to a shader when rendering
 * @memberof rendering
 */
export declare class BindGroupSystem implements System {
    /** @ignore */
    static extension: {
        readonly type: readonly [ExtensionType.WebGPUSystem];
        readonly name: "bindGroup";
    };
    private readonly _renderer;
    private _hash;
    private _gpu;
    constructor(renderer: WebGPURenderer);
    protected contextChange(gpu: GPU): void;
    getBindGroup(bindGroup: BindGroup, program: GpuProgram, groupIndex: number): GPUBindGroup;
    private _createBindGroup;
    destroy(): void;
}
