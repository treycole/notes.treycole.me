import type { ExtensionMetadata } from 'pixi.js/lib/extensions/Extensions';
import type { Effect } from 'pixi.js/lib/scene/container/Effect';
import type { PoolItem } from 'pixi.js/lib/utils/pool/Pool';
export declare class ColorMask implements Effect, PoolItem {
    static extension: ExtensionMetadata;
    priority: number;
    mask: number;
    pipe: string;
    constructor(options: {
        mask: number;
    });
    init(mask: number): void;
    destroy(): void;
    static test(mask: any): boolean;
}
