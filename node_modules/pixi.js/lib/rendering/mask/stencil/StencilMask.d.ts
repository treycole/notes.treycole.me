import { Container } from 'pixi.js/lib/scene/container/Container';
import type { ExtensionMetadata } from 'pixi.js/lib/extensions/Extensions';
import type { Point } from 'pixi.js/lib/maths/point/Point';
import type { Bounds } from 'pixi.js/lib/scene/container/bounds/Bounds';
import type { Effect } from 'pixi.js/lib/scene/container/Effect';
import type { PoolItem } from 'pixi.js/lib/utils/pool/Pool';
export declare class StencilMask implements Effect, PoolItem {
    static extension: ExtensionMetadata;
    priority: number;
    mask: Container;
    pipe: string;
    constructor(options: {
        mask: Container;
    });
    init(mask: Container): void;
    reset(): void;
    addBounds(bounds: Bounds, skipUpdateTransform: boolean): void;
    addLocalBounds(bounds: Bounds, localRoot: Container): void;
    containsPoint(point: Point, hitTestFn: (container: Container, point: Point) => boolean): boolean;
    destroy(): void;
    static test(mask: any): boolean;
}
